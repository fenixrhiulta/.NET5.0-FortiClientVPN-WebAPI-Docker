# forticlient-vpn-container-webapi-database

# How to use:
- Open docker-compose.yml and change the settings on **REPLACEHERE*
- Run docker-compose up
- Access http://localhost:5002/swagger

Note: HTTPS is disabled
